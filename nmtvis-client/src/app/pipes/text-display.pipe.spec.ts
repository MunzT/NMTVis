import { TextDisplayPipe } from './text-display.pipe';

describe('TextDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new TextDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
